import type { Network } from "../state/networks/types";

export const NETWORK_OPTIONS: Network[] = [
  {
    name: "Testnet",
    url: "wss://test.finney.opentensor.ai:443",
    key: "testnet",
  },
];
